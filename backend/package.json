{
  "name": "cpvault-backend",
  "version": "1.0.0",
  "description": "CP Vault API — Express + PostgreSQL + JWT",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "start": "node src/db/init.js && node src/index.js",
    "build": "cd ../frontend && npm ci && npm run build",
    "dev": "node --watch src/index.js",
    "db:init": "node src/db/init.js"
  },
  "dependencies": {
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "jsonwebtoken": "^9.0.2",
    "nodemailer": "^6.10.1",
    "pg": "^8.13.0"
  }
}
