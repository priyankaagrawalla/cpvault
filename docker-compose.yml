services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: cpvault
    ports:
      - "5432:5432"
    volumes:
      - cpvault_pg:/var/lib/postgresql/data

volumes:
  cpvault_pg:
